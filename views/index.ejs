<h2>Recently Added</h2>

<div>
    <% books.forEach(book => { %>
        <a href="/books/<%= book.id %>">
        <img src="<%= book.coverImagePath %>" width="150" height="150">
        <a href="/books/<%= book.id %>">
    <% }) %>
</div>